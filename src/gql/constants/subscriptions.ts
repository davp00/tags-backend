/**
 * Used to provide the PubSub class to modules, services
 */
export const SUBSCRIPTION_PROVIDER = 'PUB_SUB';

/**
 * Used to communicate the different events when a tag is updated, added and deleted
 */
export const UPDATE_LIST_EVENT = 'updateTagList';
